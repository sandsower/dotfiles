model = "gpt-5.5"
model_reasoning_effort = "high"

# Add per-machine trusted project roots locally after bootstrap.
# Do not trust the whole home directory in repo-backed config.

[notice.model_migrations]
"gpt-5.3-codex" = "gpt-5.4"

[features]
rmcp_client = true
memories = true

[mcp_servers.linear]
url = "https://mcp.linear.app/mcp"

[mcp_servers.memento-vault]
url = "https://memento-vault-vic.fly.dev/mcp"
bearer_token_env_var = "MEMENTO_API_KEY"

# Add private MCP servers locally after bootstrap.

[tui.model_availability_nux]
"gpt-5.5" = 2
